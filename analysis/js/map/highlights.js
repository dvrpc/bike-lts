const highlightLayers = ['lowstress-islands']

const highlightLowStress = (e, map) => {
    console.log('e ', e)
    console.log('e target props', e.target.properties)
    const filter = []
    
    // use feature state instead of setFilter
}

export { highlightLowStress, highlightLayers }